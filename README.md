# 钉钉蓝牙打卡（测试中）

## 环境初始化
- Python 解释器版本：3.9
- 激活你的 Python 打包环境（conda/venv），然后在项目根目录执行：
```bash
python setup.py
```

## 运行代码
- 激活你的 Python 打包环境（conda/venv），然后在项目根目录执行：
```bash
python main.py
```

## 代码打包
- 激活你的 Python 打包环境（conda/venv），然后在项目根目录执行：
```bash
python pack.py
```
